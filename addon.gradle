// Project-specific buildscript overrides live here so build.gradle can stay updateable.
gradle.projectsEvaluated {
    tasks.named('runClient').configure {
        maxHeapSize = '2G'
    }
    tasks.named('runObfClient').configure {
        maxHeapSize = '2G'
    }
}
